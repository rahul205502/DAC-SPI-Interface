# DAC-SPI-Interface
Verilog implementation of a 32-bit SPI Master controller designed to interface with and configure a Digital-to-Analog Converter (DAC). The module uses a Finite State Machine (FSM) to handle the 32-bit serial data transaction in SPI Mode.
